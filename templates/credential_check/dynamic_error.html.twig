<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Error |
			{{ theme is defined ? theme.name : 'Credencial' }}</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
		<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
		<style>
			body {
				background: {{theme.bg_gradient|default('linear-gradient(to bottom, #1C4488 0%, #08B5B1 45%, #4BBE7E 100%)')|raw}}
				;
				min-height: 100vh;
				display: flex;
				align-items: center;
				justify-content: center;
				font-family: 'Montserrat', sans-serif;
				color: {{theme.text_color|default('#FFFFFF')}}
				;
				margin: 0;
				padding: 20px;
			}
			.error-card {
				background: {{theme.glass_effect ? 'rgba(255, 255, 255, 0.15)' : 'rgba(255, 255, 255, 0.1)'}}
				;
				backdrop-filter: blur(20px);
				-webkit-backdrop-filter: blur(20px);
				border: 2px solid{{theme.glass_effect ? 'rgba(255,255,255,0.3)' : (theme.accent_color|default('#ffffff'))}};
				border-radius: 20px;
				padding: clamp(20px, 5vw, 40px);
				text-align: center;
				max-width: 500px;
				width: 100%;
				box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
			}
			.error-icon {
				font-size: clamp(40px, 10vw, 60px);
				margin-bottom: 20px;
				color: {{theme.accent_color|default('#ffffff')}}
				;
				{% if theme.glass_effect %}
					color: #FFD700;
				{% endif %}
			}
			h3 {
				font-size: clamp(1.2rem, 4vw, 1.75rem);
			}
			p {
				font-size: clamp(0.9rem, 3vw, 1.1rem);
			}
		</style>
	</head>
	<body>
		<div class="error-card">
			<div class="error-icon">⚠️</div>
			<h3>Ha ocurrido un problema</h3>
			<p class="mt-3">{{ error }}</p>
		</div>
	</body>
</html>
